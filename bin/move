#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
puts "Welcome to Tic Tac Toe!"
puts "Pick a move 1-9!"
# index = input_to_index(gets.strip)
# space = gets.strip
# index = input_to_index(space)
# move(board,index)
# display_board(board)
display_board(move(input_to_index(gets.strip)))
